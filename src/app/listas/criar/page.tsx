import { PageWrapper } from '~/components/PageWrapper';

export default function CreateNewListPage() {
  return (
    <PageWrapper title="Criar lista" subtitle="">
      <section>Criar lista</section>
    </PageWrapper>
  );
}
